# Example secrets file.
#
# Copy this file to secrets.yaml and fill in real values. secrets.yaml is
# git-ignored and must never be committed.
#
# This example is also used by CI (see .github/workflows/config-check.yml)
# to generate a throwaway secrets.yaml so the config validator can resolve
# !secret references. The values below are placeholders only.

postgres_connection_string: "postgresql://ha_user:changeme@localhost:5432/ha_db"

snapper_ben_work_resource: "https://catchy.nz/api/snapper/0000000000000000"
snapper_ben_work_payload: '{"cvv":"000"}'

snapper_ben_personal_resource: "https://catchy.nz/api/snapper/0000000000000000"
snapper_ben_personal_payload: '{"cvv":"000"}'

snapper_ness_resource: "https://catchy.nz/api/snapper/0000000000000000"
snapper_ness_payload: '{"cvv":"000"}'
